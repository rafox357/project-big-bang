import MessengerDemo from '../components/interactive/MessengerDemo';

export const demoComponents: Record<string, any> = {
  'secure-messaging-app': MessengerDemo,
  // Add more demo components as they become available
};
