---
---
<section id="vision" class="vision">
  <div class="vision-content">
    <h2>Our Cosmic Vision</h2>
    <p>In the vast digital universe, we're creating a new constellation of technology where:</p>
    <div class="vision-grid">
      <div class="vision-item">
        <h3>🌟 Digital Sovereignty</h3>
        <p>Your digital universe, your control</p>
      </div>
      <div class="vision-item">
        <h3>🌌 Universal Integration</h3>
        <p>Seamless harmony across all dimensions</p>
      </div>
      <div class="vision-item">
        <h3>🛡️ Quantum Security</h3>
        <p>Unbreakable protection at every level</p>
      </div>
      <div class="vision-item">
        <h3>🚀 Infinite Freedom</h3>
        <p>Boundless possibilities for every user</p>
      </div>
    </div>
  </div>
</section>

<style>
  .vision {
    padding: 6rem 2rem;
    background: linear-gradient(180deg, #1a1b26, #13151a);
    position: relative;
    overflow: hidden;
  }

  .vision::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 1px;
    background: linear-gradient(90deg, transparent, #00e5ff, transparent);
  }

  .vision-content {
    max-width: 1200px;
    margin: 0 auto;
    position: relative;
  }

  h2 {
    text-align: center;
    font-size: 3rem;
    margin-bottom: 2rem;
    background: linear-gradient(45deg, #00e5ff, #ff3d00);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
  }

  .vision-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 2rem;
    margin-top: 3rem;
  }

  .vision-item {
    padding: 2rem;
    border-radius: 16px;
    background: rgba(0, 229, 255, 0.05);
    border: 1px solid rgba(0, 229, 255, 0.1);
    transition: all 0.3s;
    position: relative;
    overflow: hidden;
  }

  .vision-item::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: linear-gradient(45deg, transparent, rgba(0, 229, 255, 0.1), transparent);
    transform: translateX(-100%);
    transition: transform 0.6s;
  }

  .vision-item:hover::before {
    transform: translateX(100%);
  }

  .vision-item h3 {
    color: #00e5ff;
    margin-bottom: 1rem;
    font-size: 1.5rem;
  }

  .vision-item p {
    color: rgb(var(--accent-light));
    font-size: 1.1rem;
  }
</style>